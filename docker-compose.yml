services:
  mongodb:
    image: mongo:7
    container_name: vedas-mongodb
    ports:
      - "27017:27017"
    volumes:
      - vedas_mongo_data:/data/db
    restart: unless-stopped

volumes:
  vedas_mongo_data:
